# Spines & Starlight / The Little Bookshop — runtime dependencies
# Install with:  pip install -r requirements.txt
#
# pygame-ce is the community-maintained fork of pygame; it provides the same
# `import pygame` module. The classic `pygame` package also works if preferred.
pygame-ce>=2.5
